CREATE TABLE Nome_da_table (
   Primeiro_nome INT NOT NULL AUTO_INCREMENT,
   Segundo_nome  VARCHAR(100) NOT NULL,
   Terceiro_nome VARCHAR(40) NOT NULL,
   Quarto_nome   VARCHAR(20) NOT NULL,
   numero DATE, --(0000/00/00) em modo de data
   PRIMARY KEY ( tutorial_id )
);